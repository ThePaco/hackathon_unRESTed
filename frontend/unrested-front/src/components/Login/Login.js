import React from 'react';
import PropTypes from 'prop-types';
import './Login.scss';

const Login = () => (
  <div className="Login">
    Login Component
  </div>
);

Login.propTypes = {};

Login.defaultProps = {};

export default Login;
